{
  "name": "organization-scaner",
  "version": "0.1.0",
  "private": true,
  "description": "CLI tool for scanning organization data with Puppeteer and XLSX.",
  "main": "dist/index.js",
  "bin": {
    "organization-scaner": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "puppeteer": "^24.42.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
